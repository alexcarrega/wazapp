{
	"name": "WazApp",
	"version": "3.0.0",
	"description": "Whatsapp Custom Client",
	"main": "index.js",
	"scripts": {
		"start": "electron .",
		"build-linux64": "electron-packager . WazApp --overwrite --asar --platform=linux --arch=x64 --icon=assets/icons/icon.png --prune=true --out=release",
		"build-linux32": "electron-packager . WazApp --overwrite --asar --platform=linux --arch=ia32 --icon=assets/icons/icon.png --prune=true --out=release",
		"package-linux": "electron-packager . WazApp --overwrite --asar=true --platform=linux --arch=x64 --icon=assets/icons/png/1024x1024.png --prune=true --out=release"
	},
	"repository": {
		"type": "git",
		"url": "https://github.com/alexcarrega/wazapp/"
	},
	"keywords": [
		"electron",
		"whatsapp",
		"linux",
		"debian",
		"ubuntu",
		"fedora",
		"whatsapp works with google chrome 36+",
		"whatsapp custom client"
	],
	"license": "CC0-1.0",
	"devDependencies": {
		"electron": "^4.1.1"
	},
	"dependencies": {
		"electron-notification-shim": "^1.1.0",
		"electron-open-link-in-browser": "^1.0.2",
		"electron-packager": "^13.1.1",
		"electron-settings": "^3.2.0",
		"electron-windows-badge": "^1.0.5"
	}
}
